name: Deploy docs to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install MkDocs and plugins
        run: pip install -r requirements.txt

      # Landing pages are staged from README.md -> index.md so README.md stays
      # canonical for GitHub direct viewing. The same script runs locally before
      # `mkdocs build`, so CI and local staging cannot drift apart.
      - name: Stage per-language index pages
        run: ./stage_index.sh

      - name: Build site
        run: mkdocs build --strict

      - name: Check internal links
        run: python3 check_links.py

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
